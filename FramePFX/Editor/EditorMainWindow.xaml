<v:WindowEx x:Class="FramePFX.Editor.EditorMainWindow"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
            xmlns:ap="clr-namespace:FramePFX.AttachedProperties"
            xmlns:converters="clr-namespace:FramePFX.Converters"
            xmlns:acs="clr-namespace:FramePFX.AdvancedContextService"
            xmlns:s="clr-namespace:FramePFX.Shortcuts"
            xmlns:sb="clr-namespace:FramePFX.Shortcuts.Bindings"
            xmlns:ui="clr-namespace:FramePFX.ResourceManaging.UI"
            xmlns:controls="clr-namespace:FramePFX.Editor.Timeline.Controls"
            xmlns:core="clr-namespace:FramePFX.Core;assembly=FramePFX.Core"
            xmlns:wpf="clr-namespace:SkiaSharp.Views.WPF;assembly=SkiaSharp.Views.WPF"
            xmlns:v="clr-namespace:FramePFX.Views"
            xmlns:clips="clr-namespace:FramePFX.Core.Editor.ViewModels.Timeline.Clips;assembly=FramePFX.Core"
            xmlns:timeline="clr-namespace:FramePFX.Core.Editor.ViewModels.Timeline;assembly=FramePFX.Core"
            xmlns:viewModels="clr-namespace:FramePFX.Core.Editor.ViewModels;assembly=FramePFX.Core"
            xmlns:helpers="clr-namespace:FramePFX.Controls.Helpers"
            xmlns:controls1="clr-namespace:FramePFX.Controls"
            xmlns:rm="clr-namespace:FramePFX.Core.Editor.ResourceManaging;assembly=FramePFX.Core"
            mc:Ignorable="d"
            d:DataContext="{d:DesignInstance viewModels:VideoEditorViewModel}"
            Title="Bootleg sony vegas (FramePFX, or Frame Pixel Frame X i guess... PixelFrame?)" Width="1408" Height="792"
            Style="{StaticResource CustomWindowStyle}"
            Background="{DynamicResource AREghZyBrush.Tone2.Background.Static}"
            BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}"
            s:UIFocusGroup.IsGlobalShortcutFocusTarget="True"
            s:UIFocusGroup.FocusGroupPath="Application/Editor">
    <Window.Resources>
        <converters:NullToBoolConverter x:Key="NullToDisabledConverter" NullValue="False" NonNullValue="True"/>
        <Style TargetType="{x:Type Slider}">
            <Setter Property="Stylus.IsPressAndHoldEnabled" Value="false" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="BorderBrush" Value="Transparent" />
            <Setter Property="Foreground" Value="{DynamicResource SliderThumb.Static.Foreground}" />
            <Setter Property="Template" Value="{StaticResource SliderHorizontal}" />
            <Style.Triggers>
                <Trigger Property="Orientation" Value="Vertical">
                    <Setter Property="Template" Value="{DynamicResource SliderVertical}" />
                </Trigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    <Grid>
        <Menu VerticalAlignment="Top" Background="{DynamicResource AREghZyBrush.Tone4.Border.Static}" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" BorderThickness="0 0 0 1">
            <MenuItem Header="File">
                <MenuItem Header="New Project" Command="{Binding NewProjectCommand, Mode=OneTime}"/>
            </MenuItem>
        </Menu>
        <Grid Margin="0,19,0,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="70*"/>
                <RowDefinition Height="5"/>
                <RowDefinition Height="75*"/>
            </Grid.RowDefinitions>
            <GridSplitter VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Row="1" Background="{DynamicResource AREghZyBrush.Tone4.Border.Static}" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" BorderThickness="0 1 0 1" SnapsToDevicePixels="True"/>
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="10*"/>
                    <ColumnDefinition Width="5"/>
                    <ColumnDefinition Width="7*"/>
                </Grid.ColumnDefinitions>
                <GridSplitter ResizeDirection="Columns" HorizontalAlignment="Stretch" VerticalAlignment="Stretch" BorderThickness="1 0 1 0" Background="{DynamicResource AREghZyBrush.Tone4.Border.Static}" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" Grid.Column="1" Margin="0,-1" SnapsToDevicePixels="True"/>
                <Grid Grid.Column="2" DataContext="{Binding Playback}">
                    <Border VerticalAlignment="Bottom" Height="35" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" BorderThickness="0 1 0 0">
                        <Grid>
                            <Button Content="Fit to Scale" Width="75" HorizontalAlignment="Left" Padding="1,0,0,0" Margin="5,5,0,5" Click="OnFitContentToWindowClick"/>
                            <CheckBox IsChecked="{Binding ElementName=VPViewBox, Path=PanToCursorOnUserZoom}" Content="Zoom to cursor" Width="110" HorizontalAlignment="Left" Padding="1,0,0,0" Margin="85,5,0,5"/>
                            <StackPanel Margin="0,5" Orientation="Horizontal" HorizontalAlignment="Center">
                                <Button Content="Play/Pause" Width="70" HorizontalAlignment="Left" Padding="1,0,0,0" Command="{Binding TogglePlayCommand, Mode=OneTime}"/>
                                <Button Width="26" HorizontalAlignment="Left" Padding="1,0,0,0" Command="{Binding PlayCommand, Mode=OneTime}" Margin="5,0,0,0">
                                    <Polygon Width="16" Height="16" Points="0,16 0,0 16,8" Fill="{DynamicResource AREghZyBrush.ColourfulGlyph.Static}"/>
                                </Button>
                                <Button Width="26" HorizontalAlignment="Left" Command="{Binding PauseCommand, Mode=OneTime}" Margin="5,0,0,0">
                                    <Grid Width="14" Height="16">
                                        <Rectangle Width="4" HorizontalAlignment="Left" Fill="{DynamicResource AREghZyBrush.ColourfulGlyph.Static}"/>
                                        <Rectangle Width="4" HorizontalAlignment="Right" Fill="{DynamicResource AREghZyBrush.ColourfulGlyph.Static}"/>
                                    </Grid>
                                </Button>
                            </StackPanel>
                            <Button Content="Settings" Width="75" HorizontalAlignment="Right" Padding="3,0,2,0" DataContext="{Binding Editor.ActiveProject}" Command="{Binding OpenSettingsCommand, Mode=OneTime}" Margin="0,5,5,5"/>
                        </Grid>
                    </Border>
                    <controls1:FreeMoveViewPortV2 DataContext="{Binding Editor.ActiveProject}" x:Name="VPViewBox" Margin="0,0,0,35" ClipToBounds="True">
                        <wpf:SKElement Width="{Binding Settings.Width, FallbackValue=0}" Height="{Binding Settings.Height, FallbackValue=0}"
                                       x:Name="ViewPortElement" PaintSurface="OnPaintViewPortSurface"/>
                    </controls1:FreeMoveViewPortV2>
                </Grid>
                <Border DataContext="{Binding ActiveProject, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ., Converter={StaticResource NullToDisabledConverter}, Mode=TwoWay}" Background="{DynamicResource AREghZyBrush.Tone2.Background.Static}">
                    <TabControl BorderThickness="1 1 0 0" Margin="-1,-1,0,0" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}">
                        <TabItem Header="Files and Resources" DataContext="{Binding ResourceManager}">
                            <Grid s:UIFocusGroup.FocusGroupPath="Application/Editor/Resources" acs:AdvancedContextMenu.ContextGenerator="{x:Static rm:ResourceContextGenerator.Instance}">
                                <ui:ResourceListControl FileDropNotifier="{Binding .}" ItemsSource="{Binding Resources, Mode=OneTime}" BorderThickness="0" helpers:SelectorHelper.SelectedItems="{Binding SelectedItems}" helpers:SelectorHelper.UpdateSelectedItemsOnChange="True">
                                    <ui:ResourceListControl.ItemsPanel>
                                        <ItemsPanelTemplate>
                                            <WrapPanel IsItemsHost="True" Orientation="Horizontal"/>
                                        </ItemsPanelTemplate>
                                    </ui:ResourceListControl.ItemsPanel>
                                    <ui:ResourceListControl.ItemContainerStyle>
                                        <Style TargetType="{x:Type ui:ResourceItemControl}" BasedOn="{StaticResource {x:Type ui:ResourceItemControl}}">
                                            <Setter Property="UniqueIDHeader" Value="{Binding UniqueId, UpdateSourceTrigger=PropertyChanged}"/>
                                        </Style>
                                    </ui:ResourceListControl.ItemContainerStyle>
                                    <ui:ResourceListControl.ItemTemplate>
                                        <DataTemplate>
                                            <TextBlock Text="{Binding Path=UniqueId}"/>
                                        </DataTemplate>
                                    </ui:ResourceListControl.ItemTemplate>
                                </ui:ResourceListControl>
                                <!--
                                <Grid Background="Black" HorizontalAlignment="Stretch">
                                    <controls1:FreeMoveViewPortV2 x:Name="OK">
                                        <Button Content="OK LOL" Width="100" Height="100" VerticalAlignment="Center"/>
                                    </controls1:FreeMoveViewPortV2>
                                    <StackPanel>
                                        <TextBlock Text="{Binding ElementName=OK, Path=HorizontalOffset, StringFormat='Horz = {0}'}"/>
                                        <TextBlock Text="{Binding ElementName=OK, Path=VerticalOffset, StringFormat='Vert = {0}'}"/>
                                    </StackPanel>
                                </Grid>
                                -->
                            </Grid>
                        </TabItem>
                        <TabItem Header="Clip Properties">
                            <!--
                            <Grid Grid.IsSharedSizeScope="True">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="150" SharedSizeGroup="SharedColumn0"/>
                                    <ColumnDefinition Width="*" SharedSizeGroup="SharedColumn1"/>
                                </Grid.ColumnDefinitions>
                                <ItemsControl x:Name="ClipPropertyPanelList" Grid.ColumnSpan="2" HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch">
                                </ItemsControl>
                            </Grid>
                            -->

                            <ScrollViewer HorizontalScrollBarVisibility="Auto" 
                                          VerticalScrollBarVisibility="Auto" CanContentScroll="False" 
                                          helpers:ScrollViewerHelper.UseHorizontalScrollWheel="True" 
                                          Background="{DynamicResource AREghZyBrush.Tone2.Background.Static}">
                                <ItemsControl x:Name="ClipPropertyPanelList" HorizontalContentAlignment="Stretch" HorizontalAlignment="Stretch">
                                </ItemsControl>
                            </ScrollViewer>

                            <!-- 
                                Maybe create some sort of base user control called "PropertyPage" which maps to a specific class/type of clip,
                                then when a selected item changes, iterate through the type hierarchy and generate the pages accordingly?
                            -->
                            <!-- d:DataContext="{d:DesignInstance sources:LoopbackSourceViewModel}" -->
                            <!--
                            <Grid x:Name="SettingsPanel" Background="{DynamicResource AREghZyBrush.Tone2.Background.Static}">
                                <ScrollViewer VerticalScrollBarVisibility="Auto">
                                    <ContentPresenter Content="{Binding}">
                                        <ContentPresenter.Resources>
                                            <DataTemplate DataType="{x:Type clips1:ImageClipViewModel}">
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type clips1:SquareClipViewModel}">
                                            </DataTemplate>
                                            <DataTemplate DataType="{x:Type clips1:VideoClipViewModel}">
                                            </DataTemplate>
                                        </ContentPresenter.Resources>
                                    </ContentPresenter>
                                </ScrollViewer>
                            </Grid>
                            -->
                        </TabItem>
                    </TabControl>
                </Border>
            </Grid>
            <Grid Grid.Row="2">
                <Border VerticalAlignment="Bottom" Height="20" BorderBrush="{DynamicResource AREghZyBrush.Tone4.Border.Static}" BorderThickness="0 1 0 0">
                    <!-- Status bar -->
                    <Border Background="{DynamicResource AREghZyBrush.Tone4.Border.Static}" Margin="0,-1,0,0">
                        <StackPanel Orientation="Horizontal">
                            <TextBlock VerticalAlignment="Center" x:Name="NotificationBarTextBlock" Padding="3,0"/>
                        </StackPanel>
                    </Border>
                </Border>
                <Border DataContext="{Binding ActiveProject, UpdateSourceTrigger=PropertyChanged}"
                        IsEnabled="{Binding ., Converter={StaticResource NullToDisabledConverter}, Mode=TwoWay}" Margin="0,0,0,20"
                        s:UIFocusGroup.FocusGroupPath="Application/Editor/Timeline">
                    <Border.InputBindings>
                        <sb:ShortcutCommandBinding ShortcutAndUsageId="Application/Editor/Timeline/LayerControllers/DeleteSelectedLayer" Command="{Binding Timeline.RemoveSelectedLayersCommand}"/>
                        <sb:ShortcutCommandBinding ShortcutAndUsageId="Application/Editor/Timeline/Surface/PlayPauseAction" Command="{Binding Editor.Playback.TogglePlayCommand}"/>
                    </Border.InputBindings>
                    <Grid DataContext="{Binding Timeline}">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="40"/>
                            <RowDefinition Height="*"/>
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="5*"/>
                            <ColumnDefinition Width="5"/>
                            <ColumnDefinition Width="20*"/>
                        </Grid.ColumnDefinitions>
                        <Border x:Name="PART_TimestampBoard" Background="{DynamicResource AREghZyBrush.Tone3.Background.Static}" BorderThickness="0 0 0 1" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}">
                            <Grid>
                                <TextBlock FontSize="16" Text="{Binding PlayHeadFrame, UpdateSourceTrigger=PropertyChanged}" VerticalAlignment="Center" HorizontalAlignment="Left" Margin="10 0 0 0"/>
                            </Grid>
                        </Border>
                        <Border s:UIFocusGroup.FocusGroupPath="Application/Editor/Timeline/LayerControllers" Grid.Column="0" BorderThickness="0 0 0 0" Background="{DynamicResource AREghZyBrush.Tone3.Background.Static}" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" Grid.Row="1"/>
                        <ListBox s:UIFocusGroup.FocusGroupPath="Application/Editor/Timeline/LayerControllers"
                                 ItemsSource="{Binding Layers, Mode=OneTime}" 
                                 SelectedItem="{Binding PrimarySelectedLayer, UpdateSourceTrigger=PropertyChanged}"
                                 helpers:SelectorHelper.SelectedItems="{Binding SelectedLayers}"
                                 helpers:SelectorHelper.UpdateSelectedItemsOnChange="True"
                                 SelectionMode="Extended"
                                 Grid.Column="0" Background="Transparent" BorderBrush="Transparent" 
                                 HorizontalContentAlignment="Stretch" Grid.Row="1"
                                 Padding="0" x:Name="LayerListBox"
                                 ScrollViewer.HorizontalScrollBarVisibility="Visible"
                                 ScrollViewer.CanContentScroll="False"
                                 BorderThickness="0 0 0 0">
                            <ListBox.Template>
                                <ControlTemplate TargetType="{x:Type ListBox}">
                                    <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Background="{TemplateBinding Background}" Padding="0" SnapsToDevicePixels="true">
                                        <ScrollViewer CanContentScroll="False" x:Name="PART_ScrollViewer" Focusable="false" Padding="{TemplateBinding Padding}" converters:VerticalScrollBinder.BindGroup="TimelineScrollGroup">
                                            <ItemsPresenter SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" Margin="0"/>
                                        </ScrollViewer>
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsEnabled" Value="false">
                                            <Setter Property="Background" TargetName="Bd" Value="{DynamicResource ListBox.Disabled.Background}" />
                                            <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource ListBox.Disabled.Border}" />
                                        </Trigger>
                                        <MultiTrigger>
                                            <MultiTrigger.Conditions>
                                                <Condition Property="IsGrouping" Value="true" />
                                                <Condition Property="VirtualizingPanel.IsVirtualizingWhenGrouping" Value="false" />
                                            </MultiTrigger.Conditions>
                                            <Setter Property="ScrollViewer.CanContentScroll" Value="false" />
                                        </MultiTrigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </ListBox.Template>
                            <ItemsControl.ItemContainerStyle>
                                <Style TargetType="ListBoxItem" BasedOn="{StaticResource {x:Type ListBoxItem}}">
                                    <Setter Property="Height" Value="{Binding Height, UpdateSourceTrigger=PropertyChanged, FallbackValue=60}"/>
                                    <Setter Property="MinHeight" Value="{Binding MinHeight, UpdateSourceTrigger=PropertyChanged, FallbackValue=40}"/>
                                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="BorderThickness" Value="0"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AREghZyBrush.Tone5.Border.Static}"/>
                                    <Setter Property="Padding" Value="0"/>
                                    <Setter Property="Margin" Value="0"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type ListBoxItem}">
                                                <Border x:Name="Bd" BorderBrush="{TemplateBinding BorderBrush}" Background="{TemplateBinding Background}" Padding="{TemplateBinding Padding}" SnapsToDevicePixels="true" BorderThickness="0 0 0 1">
                                                    <Grid d:DataContext="{d:DesignInstance timeline:LayerViewModel}">
                                                        <Rectangle Fill="{Binding LayerColour}" Margin="0" Width="18" VerticalAlignment="Stretch" HorizontalAlignment="Left"/>
                                                        <Grid Margin="23,4,6,4">
                                                            <TextBlock Text="{Binding Name, UpdateSourceTrigger=PropertyChanged}" ap:AttachedInteractivity.DoubleClickCommand="{Binding RenameLayerCommand, Mode=OneTime}" FontSize="14" TextWrapping="NoWrap" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="20"/>
                                                            <Grid Margin="0,20,0,0">
                                                                <ContentPresenter HorizontalAlignment="Stretch" VerticalAlignment="Stretch" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}" />
                                                            </Grid>
                                                        </Grid>
                                                        <Thumb DragDelta="OnTopThumbDrag" Margin="0,0,0,0" VerticalAlignment="Top" Background="Transparent" BorderThickness="0" Height="4" Cursor="SizeNS"/>
                                                        <Thumb DragDelta="OnBottomThumbDrag" Margin="0,0,0,0" VerticalAlignment="Bottom" Background="Transparent" BorderThickness="0" Height="4" Cursor="SizeNS"/>
                                                    </Grid>
                                                </Border>
                                                <ControlTemplate.Triggers>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="IsMouseOver" Value="True" />
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Item.MouseOver.Background}" />
                                                        <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Item.MouseOver.Border}" />
                                                    </MultiTrigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="Selector.IsSelectionActive" Value="False" />
                                                            <Condition Property="IsSelected" Value="True" />
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Background}" />
                                                        <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Item.SelectedInactive.Border}" />
                                                    </MultiTrigger>
                                                    <MultiTrigger>
                                                        <MultiTrigger.Conditions>
                                                            <Condition Property="Selector.IsSelectionActive" Value="True" />
                                                            <Condition Property="IsSelected" Value="True" />
                                                        </MultiTrigger.Conditions>
                                                        <Setter Property="Background" TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Background}" />
                                                        <Setter Property="BorderBrush" TargetName="Bd" Value="{DynamicResource Item.SelectedActive.Border}" />
                                                    </MultiTrigger>
                                                    <Trigger Property="IsEnabled" Value="False">
                                                        <Setter Property="TextElement.Foreground" TargetName="Bd" Value="{DynamicResource AREghZyBrush.Foreground.Disabled}" />
                                                    </Trigger>
                                                </ControlTemplate.Triggers>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </ItemsControl.ItemContainerStyle>
                            <ItemsControl.Resources>
                                <DataTemplate DataType="{x:Type timeline:LayerViewModel}">
                                    <Grid>
                                        <TextBlock Text="Opacity: " HorizontalAlignment="Left" VerticalAlignment="Center"/>
                                        <Slider VerticalAlignment="Center" Margin="51,0,0,0" Minimum="0" Maximum="1" Value="{Binding Opacity, UpdateSourceTrigger=PropertyChanged}" SmallChange="0.1" TickFrequency="0.1"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.Resources>
                        </ListBox>
                        <Border Visibility="Collapsed" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" BorderThickness="0 0 0 1" Grid.Column="0" Height="1" VerticalAlignment="Bottom" Grid.Row="1"/>
                        <controls:TimelineControl x:Name="TheTimeline"
                                                  ItemsSource="{Binding Layers, Mode=OneTime}"
                                                  Grid.Column="2" BorderThickness="0"
                                                  BorderBrush="{DynamicResource AREghZyBrush.Tone2.Border.Static}"
                                                  MaxDuration="{Binding MaxDuration, UpdateSourceTrigger=PropertyChanged}"
                                                  Grid.RowSpan="2"
                                                  s:UIFocusGroup.FocusGroupPath="Application/Editor/Timeline/Surface">
                            <controls:TimelineControl.Style>
                                <Style TargetType="{x:Type controls:TimelineControl}">
                                    <Setter Property="Background" Value="{DynamicResource AREghZyBrush.Tone2.Background.Static}"/>
                                    <Setter Property="ScrollViewer.CanContentScroll" Value="False"/>
                                    <Setter Property="ScrollViewer.HorizontalScrollBarVisibility" Value="Auto" />
                                    <Setter Property="ScrollViewer.VerticalScrollBarVisibility" Value="Auto" />
                                    <Setter Property="ScrollViewer.PanningMode" Value="Both" />
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type controls:TimelineControl}">
                                                <Grid>
                                                    <Border Background="{DynamicResource AREghZyBrush.Tone1.Background.Static}"/>
                                                    <!-- A collection of TimelineLayerControls -->
                                                    <ScrollViewer x:Name="PART_ScrollViewer" Focusable="false" 
                                                                  Padding="{TemplateBinding Padding}" 
                                                                  HorizontalScrollBarVisibility="Visible"
                                                                  CanContentScroll="False"
                                                                  converters:VerticalScrollBinder.BindGroup="TimelineScrollGroup">
                                                        <!-- Handle RequestBringIntoView in the grid because there is no preview version of the event -->
                                                        <!-- Handling the event will stop the ScrollViewer scrolling to offscreen values -->
                                                        <Grid HorizontalAlignment="Left" RequestBringIntoView="OnTimelineControlContentRequestBringIntoView">
                                                            <Grid.RowDefinitions>
                                                                <RowDefinition Height="40"/>
                                                                <RowDefinition Height="*"/>
                                                            </Grid.RowDefinitions>
                                                            <Border Margin="0" Background="{TemplateBinding Background}" BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="{TemplateBinding BorderThickness}" Grid.Row="1">
                                                                <ItemsPresenter x:Name="PART_ItemsPresenter" SnapsToDevicePixels="{TemplateBinding SnapsToDevicePixels}"/>
                                                            </Border>
                                                            <Border Grid.Row="0" x:Name="PART_TimestampBoard" Margin="0" VerticalAlignment="Stretch" BorderThickness="0 0 0 1" Background="{DynamicResource AREghZyBrush.Tone3.Background.Static}" BorderBrush="{DynamicResource AREghZyBrush.Tone4.Border.Static}"/>
                                                            <controls:TimelinePlayHeadControl x:Name="PART_PlayHead" Grid.Row="0" Grid.RowSpan="2"
                                                                                              VerticalAlignment="Stretch"
                                                                                              VerticalContentAlignment="Stretch"
                                                                                              HorizontalAlignment="Left"
                                                                                              Timeline="{Binding RelativeSource={RelativeSource TemplatedParent}}"
                                                                                              FrameBegin="{Binding PlayHeadFrame, UpdateSourceTrigger=PropertyChanged}" Width="1"
                                                                                              Background="{DynamicResource AREghZyBrush.Tone7.Border.Static}"/>
                                                        </Grid>
                                                    </ScrollViewer>
                                                </Grid>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </controls:TimelineControl.Style>
                            <controls:TimelineControl.ItemContainerStyle>
                                <Style TargetType="{x:Type controls:VideoTimelineLayerControl}">
                                    <Setter Property="Background" Value="Transparent"/>
                                    <Setter Property="BorderBrush" Value="{DynamicResource AREghZyBrush.Tone5.Border.Static}"/>
                                    <Setter Property="MaxHeight" Value="{Binding MaxHeight, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Setter Property="MinHeight" Value="{Binding MinHeight, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Setter Property="DropNotifier" Value="{Binding}"/>
                                    <Setter Property="Height" Value="{Binding Height, UpdateSourceTrigger=PropertyChanged}"/>
                                    <Setter Property="ItemsSource" Value="{Binding Clips, Mode=OneTime}"/>
                                    <Setter Property="VerticalContentAlignment" Value="Stretch"/>
                                    <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                                    <Setter Property="Focusable" Value="True"/>
                                    <Setter Property="SelectedItem" Value="{Binding PrimarySelectedClip}"/>
                                    <Setter Property="helpers:SelectorHelper.SelectedItems" Value="{Binding SelectedClips}"/>
                                    <Setter Property="helpers:SelectorHelper.UpdateSelectedItemsOnChange" Value="True"/>
                                    <Setter Property="Template">
                                        <Setter.Value>
                                            <ControlTemplate TargetType="{x:Type controls:VideoTimelineLayerControl}">
                                                <Border BorderBrush="{TemplateBinding BorderBrush}" BorderThickness="0 0 0 1" SnapsToDevicePixels="True">
                                                    <!-- IsItemHost must be true in order for it's items to sync with the TimelineLayerControl's items -->
                                                    <Grid IsItemsHost="True" Background="{TemplateBinding Background}"/>
                                                </Border>
                                            </ControlTemplate>
                                        </Setter.Value>
                                    </Setter>
                                    <Setter Property="ItemContainerStyle">
                                        <Setter.Value>
                                            <!-- d:DataContext="{d:DesignInstance clips1:VideoClipViewModel}" -->
                                            <Style TargetType="{x:Type controls:TimelineVideoClipControl}">
                                                <Setter Property="Background" Value="{DynamicResource AREghZyBrush.Tone4.Background.Static}"/>
                                                <Setter Property="BorderBrush" Value="{DynamicResource AREghZyBrush.Tone5.Border.Static}"/>
                                                <Setter Property="HeaderBrush" Value="{DynamicResource AREghZyBrush.Tone5.Background.Static}"/>
                                                <Setter Property="BorderThickness" Value="1"/>
                                                <Setter Property="HorizontalAlignment" Value="Left"/>
                                                <Setter Property="VerticalAlignment" Value="Stretch"/>
                                                <Setter Property="Focusable" Value="True"/>
                                                <Setter Property="FrameBegin" Value="{Binding FrameBegin, UpdateSourceTrigger=PropertyChanged}"/>
                                                <Setter Property="FrameDuration" Value="{Binding FrameDuration, UpdateSourceTrigger=PropertyChanged}"/>
                                                <Setter Property="MinHeight" Value="{Binding Layer.MinHeight, UpdateSourceTrigger=PropertyChanged}"/>
                                                <Setter Property="MaxHeight" Value="{Binding Layer.MaxHeight, UpdateSourceTrigger=PropertyChanged}"/>
                                                <Setter Property="Height" Value="{Binding Layer.Height, UpdateSourceTrigger=PropertyChanged}"/>
                                                <Setter Property="Template">
                                                    <Setter.Value>
                                                        <ControlTemplate TargetType="{x:Type controls:TimelineVideoClipControl}">
                                                            <Grid d:DataContext="{d:DesignInstance clips:VideoClipViewModel}">
                                                                <Grid x:Name="FullElementContent" SnapsToDevicePixels="True">
                                                                    <Border Background="{Binding Layer.LayerColour}" VerticalAlignment="Top" Height="20">
                                                                        <Grid>
                                                                            <TextBlock Text="{Binding DisplayName, UpdateSourceTrigger=PropertyChanged}" FontSize="11" HorizontalAlignment="Left" VerticalAlignment="Center" Padding="4 1">
                                                                                <TextBlock.InputBindings>
                                                                                    <MouseBinding MouseAction="LeftDoubleClick" Command="{Binding EditDisplayNameCommand, Mode=OneTime}"/>
                                                                                </TextBlock.InputBindings>
                                                                            </TextBlock>
                                                                        </Grid>
                                                                    </Border>
                                                                    <Grid Margin="1,20,1,0" Background="{TemplateBinding Background}">
                                                                        <!--
                                                                        <ogl:OGLViewportControl x:Name="PART_ViewPort"/>
                                                                        -->
                                                                    </Grid>
                                                                </Grid>
                                                                <Border x:Name="OutlineBorder" BorderThickness="1" SnapsToDevicePixels="True"/>
                                                                <Thumb x:Name="PART_ThumbLeft" HorizontalAlignment="Left" Width="8" Margin="-4,0,0,0" Cursor="SizeWE" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"/>
                                                                <Thumb x:Name="PART_ThumbRight" HorizontalAlignment="Right" Width="8" Margin="0,0,-4,0" Cursor="SizeWE" Background="Transparent" BorderBrush="Transparent" BorderThickness="0"/>
                                                            </Grid>
                                                            <ControlTemplate.Triggers>
                                                                <Trigger Property="IsSelected" Value="True">
                                                                    <Setter TargetName="OutlineBorder" Property="BorderBrush" Value="GreenYellow"/>
                                                                </Trigger>
                                                                <Trigger Property="IsSelected" Value="False">
                                                                    <Setter TargetName="OutlineBorder" Property="BorderBrush" Value="{Binding BorderBrush, RelativeSource={RelativeSource Mode=TemplatedParent}}"/>
                                                                </Trigger>
                                                                <Trigger Property="IsDroppableTargetOver" Value="True">
                                                                    <Setter TargetName="OutlineBorder" Property="BorderBrush" Value="OrangeRed"/>
                                                                    <Setter TargetName="OutlineBorder" Property="BorderThickness" Value="3"/>
                                                                </Trigger>
                                                            </ControlTemplate.Triggers>
                                                        </ControlTemplate>
                                                    </Setter.Value>
                                                </Setter>
                                            </Style>
                                        </Setter.Value>
                                    </Setter>
                                </Style>
                            </controls:TimelineControl.ItemContainerStyle>
                        </controls:TimelineControl>
                        <GridSplitter Background="{DynamicResource AREghZyBrush.Tone4.Border.Static}" ResizeDirection="Columns" VerticalAlignment="Stretch" HorizontalAlignment="Stretch" Grid.Column="1" BorderBrush="{DynamicResource AREghZyBrush.Tone6.Border.Static}" BorderThickness="1 0 1 0" Grid.RowSpan="2" Margin="0,-1,0,0" SnapsToDevicePixels="True"/>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</v:WindowEx>
