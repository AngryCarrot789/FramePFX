using System.Windows.Controls;

namespace FramePFX.Editor.Timeline.Controls {
    public class AutomationComboBox : ComboBox {
        // custom combo box type, just in case upgrades are needed at some point
    }
}