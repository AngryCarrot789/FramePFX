using System;

namespace FramePFX.PropertyEditing {
    public class SpecialPropertyGroupViewModel : FixedPropertyGroupViewModel {
        public SpecialPropertyGroupViewModel(Type applicableType, string id) : base(applicableType, id) {

        }
    }
}